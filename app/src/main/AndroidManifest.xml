<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.tora">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">

        <activity android:name=".ArticleActivity"></activity>
        <activity android:name=".MovementTasks" />
        <activity android:name=".SearchPage" />
        <activity android:name=".SignUpActivity2" />
        <activity android:name=".preferencesScreen" />
        <activity android:name=".movementPage" />
        <activity android:name=".userPage"></activity>
        <activity android:name=".recommendationPage" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".loginActivity"/>

        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>